{
    "name": "roku-development",
    "displayName": "Roku Development",
    "description": "An extension for debugging and deploying Roku apps for VS Code",
    "version": "0.0.1",
    "publisher": "calango.io",
    "engines": {
        "vscode": "^1.28.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.deploy"
    ],
    "main": "./out/extension",
    "contributes": {
        "commands": [
            {
                "command": "extension.deploy",
                "title": "Roku deploy"
            }
        ]
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "npm run compile && node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "typescript": "^2.6.1",
        "vscode": "^1.1.21",
        "tslint": "^5.8.0",
        "@types/node": "^8.10.25",
        "@types/mocha": "^2.2.42"
    }
}
